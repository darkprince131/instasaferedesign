# Drop these attachments here (exact filenames)

The components reference these paths. Save the images you pasted into chat as:

| Save as (in /public)        | Which attachment                                            |
|-----------------------------|-------------------------------------------------------------|
| `instasafe-mark.png`        | The square logo (orange arcs + blue sphere, no text)        |
| `architecture-idle.webp`    | ZTNA flow with GREY "Secure Access" line                    |
| `architecture-active.webp`  | ZTNA flow with GREEN "Secure Access" line                   |

Until these exist the logo/diagram slots show a fallback or broken image.
